---
description: Compteur de repos "temps depuis la dernière série" (RestSinceLastSetBar) — comportement, source de données, contraintes UI/analytics
globs: client/src/**/*.{ts,tsx}
alwaysApply: false
---

# Compteur de repos entre séries — One More

Barre fine qui se lance **automatiquement** dès qu'une performance est enregistrée et affiche le temps `m:ss` écoulé depuis la dernière série, quel que soit l'exercice consulté.

## Source unique de vérité

| Concern | Fichier |
|---|---|
| UI | [`client/src/components/RestSinceLastSetBar.tsx`](../../client/src/components/RestSinceLastSetBar.tsx) |
| Ticker + throttle onglet caché | [`client/src/hooks/use-rest-since-last-set.ts`](../../client/src/hooks/use-rest-since-last-set.ts) |
| **Dernière perf globale** (tous exercices) | [`client/src/hooks/use-latest-global-perf.ts`](../../client/src/hooks/use-latest-global-perf.ts) |
| Formatage, seuils, progression | [`client/src/lib/format-rest-elapsed.ts`](../../client/src/lib/format-rest-elapsed.ts) |
| **Durée cible utilisateur** (défaut 1:30) | [`client/src/hooks/use-rest-target-ms.ts`](../../client/src/hooks/use-rest-target-ms.ts) + `getRestTargetMs` / `setRestTargetMs` dans [`client/src/lib/storage.ts`](../../client/src/lib/storage.ts) |
| **Préservation `createdAt` à l'édition** | [`client/src/lib/activity-from-performances.ts`](../../client/src/lib/activity-from-performances.ts) (`preservePerformanceLogCreatedAt`) |
| Réglage dans Paramètres | [`client/src/components/settings/RestTimeSettingsCard.tsx`](../../client/src/components/settings/RestTimeSettingsCard.tsx) |
| **Tour first-time** (AppTour) | [`client/src/components/AppTour.tsx`](../../client/src/components/AppTour.tsx) + [`client/src/components/RestCounterTour.tsx`](../../client/src/components/RestCounterTour.tsx) |
| **Sync tour ↔ popover repos** | [`client/src/lib/rest-counter-tour-quick-edit.ts`](../../client/src/lib/rest-counter-tour-quick-edit.ts) + [`client/src/lib/rest-counter-tour-spotlight.ts`](../../client/src/lib/rest-counter-tour-spotlight.ts) |
| **Édition rapide inline** | [`client/src/components/RestTargetQuickEdit.tsx`](../../client/src/components/RestTargetQuickEdit.tsx) — zone tour : `data-tour="rest-counter-target-zone"` |
| Styles Joyride partagés | [`client/src/lib/app-tour-config.ts`](../../client/src/lib/app-tour-config.ts) |
| Analytics | `trackRestTimerDismissed` dans [`client/src/lib/analytics/performance-tracking.ts`](../../client/src/lib/analytics/performance-tracking.ts) |
| Point de montage | Header sticky de [`client/src/pages/ExerciseDetailPage.tsx`](../../client/src/pages/ExerciseDetailPage.tsx) |
| **Toast fin de repos (global)** | [`client/src/components/RestTimeFinishedToastHost.tsx`](../../client/src/components/RestTimeFinishedToastHost.tsx) + [`client/src/components/RestTimeFinishedToast.tsx`](../../client/src/components/RestTimeFinishedToast.tsx) |
| **Notif système repos (background)** | Plugin natif [`client/plugins/rest-timer`](../../client/plugins/rest-timer) via [`client/src/lib/rest-timer-local-notifications.ts`](../../client/src/lib/rest-timer-local-notifications.ts) + [`client/src/hooks/use-rest-timer-local-notifications.ts`](../../client/src/hooks/use-rest-timer-local-notifications.ts) |
| **Activation / désactivation globale** | [`client/src/hooks/use-rest-timer-enabled.ts`](../../client/src/hooks/use-rest-timer-enabled.ts) + `isRestTimerEnabled` / `setRestTimerEnabled` dans [`client/src/lib/storage.ts`](../../client/src/lib/storage.ts) (clé `one-more-rest-timer-enabled-v1`, défaut activé) |
| **Toggle header fiche exo** | [`client/src/pages/ExerciseDetailPage.tsx`](../../client/src/pages/ExerciseDetailPage.tsx) — icônes `Timer` / `TimerOff` |
| **Son fin de repos in-app** | [`playRestFinishedSound()`](../../client/src/lib/milestone-sound.ts) dans `RestTimeFinishedToastHost` |

## Règles de comportement

1. **Global à tous les exercices** — Le `createdAt` alimentant la barre vient **toujours** de `useLatestGlobalPerf()` (dernière perf loggée, tous exercices confondus, `deletedAt` exclu). Ne **jamais** repasser à `usePerformance(id).lastPerf` pour l'alimenter : la barre doit rester visible quand l'utilisateur change d'exercice.
2. **Nom de l'exo source affiché** — La barre montre `REPOS` **et** le nom de l'exercice où la perf a été loggée, résolu via `useLatestGlobalPerf().exercise`. Le hook croise `useHomeExercisesData` + `getTrackedExerciseById` (fallback storage) pour trouver le nom même si l'exo n'est plus dans la home list active.
3. **Tap = ouvrir l'exo source** — Seul le **nom d'exercice** (ligne du dessous) navigue vers `/exercise/{sourceExercise.id}`. Désactivé quand `sourceExercise.id === currentExerciseId`. La pill `RestTargetQuickEdit` et le badge `REPOS` ne naviguent pas.
4. **Auto-start** — Aucun bouton "démarrer". Enregistrer une perf via `savePerformanceAndWait` suffit : SWR revalide, `useLatestGlobalPerf` renvoie la nouvelle entrée, la barre se re-monte via `key={createdAt}` et repart à `0:00`. **Éditer** une perf existante (`updatePerformance`) ne doit **pas** reset le compteur : conserver `createdAt` d'origine via `preservePerformanceLogCreatedAt` (storage + merge SWR).
5. **Durée max d'affichage** — Masquée automatiquement après `REST_SINCE_LAST_SET_MAX_MS` (10 min). Distinct de la **durée cible** (`DEFAULT_REST_TARGET_MS` = 1:30 par défaut) qui pilote uniquement la barre de progression.
6. **Durée cible personnalisable** — Stockée en localStorage via `getRestTargetMs` / `setRestTargetMs` (clé `one-more-rest-target-ms-v1`). Bornes : `REST_TARGET_MIN_MS` (30 s) → `REST_TARGET_MAX_MS` (5 min). Réglage dans Paramètres (`RestTimeSettingsCard`, minutes + secondes par pas de 15 s). Le hook `useRestTargetMs` écoute l'événement `one-more:rest-target-changed` pour propager le changement sans reload.
7. **Barre de progression** — `getRestProgress01(elapsedMs, targetMs)` : remplissage sur la durée cible, plafonné à 100 %. Au-delà, la barre reste pleine ; le compteur `m:ss` **continue** sans reset.
8. **Fin de repos** — Triple canal :
    - **In-app (premier plan)** — toast global via `RestTimeFinishedToastHost` + `showRestTimeFinishedToast` (cliquable → fiche exo) + `playRestFinishedSound()` + haptique. Annule le timer natif pour éviter le doublon.
    - **Android (arrière-plan / écran verrouillé)** — plugin `rest-timer` : notif **ongoing** dès la sauvegarde perf (chronomètre système countdown + barre de progression, ID `42`). Alarme exacte à `createdAt + targetMs` pour la notif de fin (son `rest_timer_done`). Masquée au premier plan via `setForegroundVisible(false)`, ré-affichée au background. Tap → `/exercise/{id}`.
    - **iOS (arrière-plan / écran verrouillé)** — Live Activity (`OneMoreRestTimer` widget extension, iOS 16.2+) + notif locale de secours à la fin. Masquée au premier plan comme Android. Tap Live Activity → deep link exercice.
    - Resync via `useRestTimerLocalNotifications` si la cible change. Ne pas utiliser `@capacitor/local-notifications` pour le repos.
9. **Édition rapide inline (`RestTargetQuickEdit`)** — Popover autonome (state `open` **interne**, jamais remonté dans `RestSinceLastSetBar`). Pill ±15 s + presets 1:00–3:00. `stopPropagation` sur le trigger et le panneau pour ne pas déclencher la nav exo. Persiste via `setRestTargetMs` (même storage que Paramètres).
10. **Dismiss manuel (désactivation globale)** — Le bouton croix appelle `setRestTimerEnabled(false)` (persisté, clé `one-more-rest-timer-enabled-v1`) : même effet que le toggle `TimerOff` du header (barre masquée, toast off, Live Activity / notif native annulées). **Doit** `event.stopPropagation()` sinon le tap remonte au bouton parent et rouvre l'exo. Réactivation via le toggle header (`Timer`) ; la barre réapparaît si une perf récente existe (`useEffect` reset `dismissed` quand `enabled` repasse à `true`).
11. **Toggle activation** — Bouton header fiche exo (`Timer` / `TimerOff`) : bascule `restTimerEnabled`. Quand désactivé : pas de barre, pas de toast fin de repos, pas de timer natif (cancel via `setRestTimerLifecycleEnabled(false)`). Quand réactivé : resync via `updateRestTimerNotificationParams` dans le hook React (ne pas relancer un `sync(null)` au enable).
12. **Onglet caché** — Le ticker s'arrête sur `visibilitychange` puis resynchronise `now = Date.now()` au retour. Ne pas remplacer par un `setInterval` non-conditionnel (coût batterie / drift iOS).
13. **Motion** — Respect de `prefers-reduced-motion` : la barre de progression saute au lieu de transitionner. À conserver pour tout nouveau visuel ajouté.
14. **Accessibilité** — `role="status"`, `aria-label` via `UI.restSinceLastSetA11y` ou `UI.restTimeFinishedA11y` une fois la cible atteinte. Quand un exo source est cliquable, le `aria-label` du bouton concatène le nom pour annoncer la destination (`·`, pas de tiret cadratin). Mise à jour `sr-only` à chaque nouvelle minute (bucket) sans spammer le lecteur d'écran.
15. **Tour first-time (`RestCounterTour`)** — Une seule fois par appareil (`isRestCounterTourComplete`, clé `one-more-rest-counter-tour-complete-v1`). **Deux étapes** (`continuous`) via `AppTour` :
    - **Étape 1** — cible `[data-tour="rest-counter"]` : présente le compteur et la barre de progression (1:30 par défaut). Pas de lien Paramètres.
    - **Étape 2** — cible `[data-tour="rest-counter-target-zone"]` : ancre **fixed** calculée par [`rest-counter-tour-spotlight.ts`](../../client/src/lib/rest-counter-tour-spotlight.ts) (union pill + panneau portal). Le popover s'ouvre **normalement** (portal sous le chrono) ; le spotlight Joyride suit tag + choix du temps. Fin / abandon : `stopRestCounterTourSpotlightSync()` + `setRestCounterTourQuickEditStep2Active(false)`.
    - Reporté tant que le tour de la fiche exercice n'est pas terminé (`isExerciseDetailTourComplete`, clé `one-more-exercise-detail-tour-complete-v1`). Le chronomètre démarre **après** ce tour, sur la même page.
16. **Lifecycle natif (mutex toggle)** — [`rest-timer-local-notifications.ts`](../../client/src/lib/rest-timer-local-notifications.ts) : `lifecycleEpoch` + `enqueueNative` sérialisent cancel/start/update. Le hook appelle `setRestTimerLifecycleEnabled(lifecycleActive)` sans cleanup disable à chaque changement ; disable uniquement au unmount.
17. **iOS background** — Au passage arrière-plan, si le timer est déjà sync (`lastSyncedKey === paramsKey`), appeler **seulement** `setForegroundVisible({ visible: true })` ; ne pas enchaîner un `sync` complet (cancel+start) qui casse la Live Activity ActivityKit. Resync complet seulement si pas encore sync ou params changés.
18. **Permissions (indépendantes push gym / onboarding salle)** — Dès qu'une perf démarre le compteur (`scheduleRestFinishedLocalNotificationForEntry`, `forceUpdateRestTimerNotificationParams`), appeler `ensureRestTimerNotificationPermission()` (plugin `rest-timer` : `checkPermissions` puis `requestPermissions` si besoin). iOS : accordé si notifs locales **ou** Live Activities activées (`ActivityAuthorizationInfo`). **Ne pas** bloquer le repos sur `useGymNotificationsReady` ni `useGymOnboardingBlocksFeatures` ; la modale push des Paramètres ne remplace pas cette demande.
19. **Lifecycle natif (conditions)** — `lifecycleActive = natif && authentifié && restTimerEnabled`. Le toast (`RestTimeFinishedToastHost`) suit la même règle (auth + compteur ON), sans gate onboarding salle.

## `RestTargetQuickEdit` — contraintes Radix (régression connue)

Le popover **doit rester cliquable hors tour**. Lors de refactors :

- `<PopoverTrigger asChild>` exige un **`<button>` natif direct** en enfant, **ou** un composant avec `React.forwardRef` qui transmet ref + props au DOM. Un wrapper intermédiaire sans `forwardRef` casse silencieusement l'ouverture (clic visible, pas de `onOpenChange`).
- Ne **pas** remonter `open` / `onOpenChange` dans `RestSinceLastSetBar` pour le tour : coordination via `rest-counter-tour-quick-edit.ts` uniquement.
- Étape 2 tour : popover portal normal + ancre spotlight calculée (`rest-counter-tour-spotlight.ts`). `<PopoverTrigger asChild>` → `<button>` natif direct (ou `forwardRef`).

## À faire pour tout nouveau point de montage

Si la barre doit apparaître sur une autre page (ex. Home, History) :

- Récupérer `const latestGlobalPerf = useLatestGlobalPerf()` puis :

```tsx
<RestSinceLastSetBar
  key={latestGlobalPerf?.entry.createdAt ?? 'none'}
  createdAt={latestGlobalPerf?.entry.createdAt ?? null}
  sourceExercise={latestGlobalPerf?.exercise ?? null}
  currentExerciseId={/* id de la page si exo, sinon null */}
/>
```

- Toujours utiliser `key={createdAt}` pour que dismiss + compteur se réinitialisent au bon moment.
- Passer `currentExerciseId` (peut être `null` hors page exo) — sinon le tap re-navigue vers soi-même.
- La barre doit rester dans un header **`sticky-top-safe`** existant (voir [`mobile-safe-area.mdc`](mobile-safe-area.mdc)) ; ne pas la placer en flottant fixe (conflit avec la bottom nav et les scrims safe-area).

## Interdits

- Ne pas créer un second hook "timer de repos" — étendre `use-rest-since-last-set` / `use-latest-global-perf`.
- Ne pas alimenter la barre avec `Date.now()` local (perdrait la persistance entre navigations et remounts).
- Ne pas afficher deux barres simultanément (dédupliquer si ajoutée dans un layout global **et** une page).
- Ne pas re-tracker `rest_timer_dismissed` ailleurs que dans le handler de la croix : l'événement doit rester 1:1 avec l'action utilisateur.
- Ne pas coder en dur `10 * 60 * 1000` : importer `REST_SINCE_LAST_SET_MAX_MS`.
- Ne pas confondre durée d'affichage (10 min) et durée cible (1:30 par défaut) : deux constantes distinctes.
- Ne pas reset le compteur quand la cible est atteinte — seule la barre et le visuel changent.
- Ne pas reset le compteur à l'édition d'une perf — `preservePerformanceLogCreatedAt` obligatoire.
- Ne pas re-déclencher le toast de fin de repos pour le même `createdAt`.
- Ne pas oublier `event.stopPropagation()` sur le bouton croix, sinon le clic remonte au bouton parent et re-navigue vers l'exo source.
- Ne pas dupliquer la résolution du nom d'exo dans les composants : passer par `useLatestGlobalPerf().exercise`.
- Ne pas remettre de lien Paramètres dans le tour repos — l'édition se fait via `RestTargetQuickEdit` (étape 2).
- Ne pas bloquer les interactions sur l'étape 2 du tour (`blockTargetInteraction: false`, `disableFocusTrap: true`).
- Ne pas extraire le trigger du popover dans un sous-composant sans `forwardRef` sous `<PopoverTrigger asChild>`.
- Ne pas coupler le tour au state `open` du parent — module `rest-counter-tour-quick-edit.ts` seulement.
- Ne pas planifier la notif repos seulement au passage background (retard OS). Démarrer le timer natif à la sauvegarde perf, masquer au foreground / toast.
- Ne pas réintroduire `@capacitor/local-notifications` pour le timer de repos (utiliser le plugin `rest-timer`).
- Ne pas appeler `setRestTimerLifecycleEnabled(false)` dans le cleanup d'un effet React lié à `lifecycleActive` (provoque cancel/epoch spurious au re-enable).
- Ne pas enchaîner `setForegroundVisible(true)` + `syncRestFinishedLocalNotificationInternal` sur iOS quand le timer est déjà sync (Live Activity flicker / disparition).
- Ne pas bloquer le repos sur `useGymNotificationsReady` ou `useGymOnboardingBlocksFeatures` (onboarding salle skip, first-exercise pending, pas de salle API).
- Ne pas confondre la permission push gym (`PushNotifications`) avec celle du plugin `rest-timer` — demander via `ensureRestTimerNotificationPermission` au démarrage du compteur.

## Voir aussi

- [`copywriting-french.mdc`](copywriting-french.mdc) — ponctuation UI (pas de `--` / `—` dans les chaînes affichées).
- [`openpanel-tracking.mdc`](openpanel-tracking.mdc) — taxonomie `rest_timer_dismissed`.
- [`mobile-safe-area.mdc`](mobile-safe-area.mdc) — contraintes header sticky + safe area iOS/Android.
- [`typography.mdc`](typography.mdc) — police `font-one-more` italique tabular-nums utilisée pour le chrono.
